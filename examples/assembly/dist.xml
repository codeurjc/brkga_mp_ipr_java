<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <!-- Packages the example project as a self-contained zip for a GitHub
         release. Unzip it, then `mvn compile exec:java ...` pulls the wrapper
         (with bundled native binaries) from Maven Central. -->
    <id>dist</id>
    <formats>
        <format>zip</format>
    </formats>
    <baseDirectory>brkga_mp_ipr_java_examples</baseDirectory>
    <fileSets>
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>pom.xml</include>
                <include>README.md</include>
                <include>src/**</include>
                <include>data/**</include>
                <include>assembly/**</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>run.sh</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>
    </fileSets>
</assembly>
